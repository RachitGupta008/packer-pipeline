name: Call Packer
on: 
  push:
    paths: "gce/*"

jobs:
  packer-build:
          uses: RachitGupta008/packer-pipeline/.github/workflows/packer-container.yml@main
          with: 
              build-path: 'gce'
          secrets: inherit
  
